import enum
import client

class LiftState(int, enum.Enum):
        NEUTRAL = 0
        GOING_UP = 1
        GOING_DOWN = 2

class Lift(object):
      
   def __init__(self):
        """Constructor"""
        self.clients = list()
        self.floor_from = 1
        self.floor_to = 1
        self.state = LiftState.Neutral